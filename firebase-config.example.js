// Copy this file to firebase-config.js and fill in values from the Firebase console.
// firebase-config.js is gitignored — never commit your real config.
//
// How to get these values:
// 1. Go to https://console.firebase.google.com/
// 2. Create a project (or pick an existing one)
// 3. Project settings → General → Your apps → Web app → register app
// 4. Copy the config object fields into below.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
